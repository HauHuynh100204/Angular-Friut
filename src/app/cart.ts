
// Ví dụ với interface
export interface Carts {
  id: string
  userId: string
  productId: string;
  productName: string;
  productCode: string;
  releaseDate: string;
  price: number;
  description: string;
  starRating: number;
  imageUrl: string;
  quantity: number
};